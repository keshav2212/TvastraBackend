CREATE TABLE GALLERY (
    ID BIGINT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    NAME VARCHAR,
    OWNER_ID BIGINT,
    CONSTRAINT fk_gallery_user FOREIGN KEY (OWNER_ID) REFERENCES USERS (ID)
);