CREATE TABLE ARTIST (
    ID BIGINT PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    NAME VARCHAR,
    GALLERY_ID BIGINT,
    CONSTRAINT fk_artist_gallery FOREIGN KEY (GALLERY_ID) REFERENCES GALLERY (ID)
);